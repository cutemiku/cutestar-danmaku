# 星萌弹幕姬 all-in-one 容器环境变量示例
# 复制为 .env 后填写（docker-compose 启动时自动读取本目录 .env）
# 单容器内运行 nginx（前端 + 反代）与 uvicorn（后端），仅暴露 80 端口。

# JWT 签名密钥：必须为至少 32 位随机字符串，勿使用示例值
# 生成方式（PowerShell）: [Convert]::ToBase64String((1..48 | %{Get-Random -Max 256}) -as [byte[]])
CUTESTAR_JWT_SECRET=

# 管理员登录密码：勿使用弱口令
CUTESTAR_ADMIN_PASSWORD=

# 可选覆盖（不填使用默认值）
# CUTESTAR_ADMIN_USERNAME=admin
# CUTESTAR_ADMIN_ENTRY_PATH=ops-console-7f3a
# HTTP_PORT=8080
